name: election_patrol_officer
description: Election patrol officer app — project setup only.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^12.0.0
  permission_handler: ^11.3.1
  firebase_core: ^3.0.0
  firebase_messaging: ^15.0.0
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.2
  google_maps_flutter: ^2.10.0
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env
